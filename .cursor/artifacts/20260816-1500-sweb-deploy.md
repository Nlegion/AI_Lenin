# Deploy to sweb-ai-lenin-vps — 2026-08-16

## Outcome
**Success.** AI_Lenin running on `80.93.60.115` (`/opt/ai-lenin`), Telegram Bot API via host Xray VLESS → Beget, DeepSeek/TASS direct.

## Verified
- `rag_preflight_ok`, `sentence_transformer_loaded`, retrieval provider initialized
- `telegram_proxy_enabled` required=true; in-container `getMe` ok=True
- Telegram `sendMessage` HTTP 200; analyses published
- Generation via DeepSeek produced analyses (published to channel)
- No `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` in container
- Local `ai-lenin` container absent
- systemd: `xray` + `ai-lenin` (After=xray, SOCKS readiness)

## Ops notes
- Host: 15 GiB RAM + 4G swap; Docker 29.7.2
- SOCKS `:1080` password-auth + iptables drop from non-private ranges
- Code: `TELEGRAM_PROXY_REQUIRED` / `TELEGRAM_PROXY_URL` + `socksio`

## Follow-up (security)
Rotate Telegram bot token if logs with full bot URL were retained (httpx logs request URLs). Prefer rotating SOCKS password if it appeared in shell transcripts.
