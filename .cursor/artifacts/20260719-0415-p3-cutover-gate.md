# P3 Migration Cutover Gate

## Inputs
- A/B parity snapshot: `.cursor/artifacts/retrieval/retrieval_ab_summary_postrerun.md`
- Rollback drill: `.cursor/artifacts/retrieval/rollback_drill.md`
- Pre-cutover monitor: `.cursor/artifacts/retrieval/ab_monitor.md`
- Target gate: `overlap@5 >= 0.80`

## Results
- A/B average parity shared ratio: `0.0719`
- Pre-cutover monitor global parity mean: `0.0646` (stable across 5 rounds)
- Non-empty context ratio:
  - `ab_shadow`: `1.000`
  - `qdrant_only`: `1.000`
  - `chroma_only`: `1.000`

## Rollback Drill Verdict
- Rollback path operational at retrieval level:
  - switch to `qdrant_only` yields non-empty context,
  - switch to `chroma_only` yields non-empty context,
  - `ab_shadow` mode remains functional.

## Cutover Decision
- **Go/No-Go**: `NO-GO`
- Reason: parity is far below required `0.80` threshold.

## Required Next Actions Before Cutover
1. Increase retrieval parity via model/fusion/source policy alignment.
2. Re-run A/B monitor after each tuning batch.
3. Re-evaluate cutover only when parity threshold is met and stable.
