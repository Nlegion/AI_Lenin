retrieval_pipeline:
  enabled: true
  collection_name: "philosophy_ontology_giga_v1"
  qdrant_path: "database/qdrant_local"
  dense_model: "models/Giga-Embeddings-instruct"
  sparse_state_path: ".cursor/artifacts/qdrant/sparse_encoder_state_giga_v1.json"
  ontology_tags_path: ".cursor/artifacts/ontology/ontology_tags.tsv"
  trust_remote_code: true
  device: "auto"
  fallback_to_cpu: true
  expected_dim: 2048
  top_k: 20
  rrf_k: 60
  max_context_chunks: 7
  hyde_enabled: true
  query_rewrite_enabled: true
  query_decomposition_enabled: true
  judge_enabled: true
  judge_alpha: 0.2
  retriever_weights:
    dense: 1.0
    sparse: 0.8
    onto: 0.6
  source_boosts:
    core_self: 1.5
    influence_agree: 1.2
    influence_critical: 1.0
    contextual: 0.9
  migration:
    mode: "ab_shadow"  # qdrant_only | chroma_only | ab_shadow
    parity_min_shared_ratio: 0.25
    audit_log_path: ".cursor/artifacts/retrieval/retrieval_ab_audit.jsonl"
  chroma_top_k: 7

dialectical_orchestration:
  enabled: false
  r1_limit: 4
  r2_limit: 3
  r3_limit: 3
  require_r1: true
  fail_on_empty_r1: false
  include_axes_in_query: true
  include_modality_suffix: true
  axes_lemma_enabled: true
  fallback_to_legacy_context: true
  # Calibrate after Phase 0 latency probe (wall >= ~3 * p95_slot + margin).
  slot_timeout_sec: 3.0
  retrieve_wall_timeout_sec: 4.0
  widen_factor: 3
  max_retries: 2
  r1_modality_suffix: ""  # usually unused for R1
  r2_modality_suffix: "поддержка"
  r3_modality_suffix: "критика"
  short_lead_chars: 200
