generation:
  persona_model: "base_strong"
  provider: "llama"
  server_url: "http://127.0.0.1:8080"
  comparison_seed: 42
  safety:
    post_filter: true
    fallback:
      enabled: false
      incident_threshold: 5
      window_events: 50
      audit_log_path: ".cursor/artifacts/safety/dryrun_audit.jsonl"
  backends:
    base_strong:
      model_name: "ai-sage/GigaChat3-10B-A1.8B"
      model_path: "models/gigachat3/GigaChat3-10B-A1.8B-q6_k.gguf"
      api_style: "chat_completions"
      ctx_size: 4096
      n_gpu_layers: 99
      threads: 4
      temperature: 0.4
      top_p: 0.9
      repetition_penalty: 1.25
      max_tokens: 512
      seed: 42
      max_context_chars: 3000
      thinking_mode: "disabled"
      # reasoning_effort used only when thinking_mode=enabled (DeepSeek)
      # reasoning_effort: "low"
